//Métodos servem pra dividir blocos de funcionalidade do código

public class Metodo {
    // public static void main(String[] args) {      

    //     if (calculaVelocidadeAtual(tempoAtual, velocidadeAtual) > 80) {
    //         velocidadeAtual = velocidadeAtual-1f;// diminui a velocidade atual
    //     }// excesso de velocidade
    // }
    // public static float calculaVelocidadeAtual(long tAtual, float vAtual){
    //     float velocidade = (vAtual-velocidadeAnterior)/(tAtual-tempoAnterior);
    //     velocidadeAnterior = vAtual;
    //     tempoAnterior = tAtual;
    //     return velocidade;
    // }// calcula a velocidade Atual
}
